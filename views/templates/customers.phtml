<!-- <?php var_dump($data); ?> -->
<main id="customers">

    <a href="../index.php" id="retour" class="btn"><i class="fa-solid fa-chevron-left"></i></a>
    <h1>Utilisateurs</h1>
    
    <section>
        <a href="index.php?route=addCustomer" class="btn colorGreen">Ajout Utilisateur</a>
        <a href="index.php?route=new-room" class="btn colorGreen">Nouveau salon</a>
        <!--<img src="assets/codes_barres/<?= $imgCodeBarre ?>" alt="code barre" />-->
    </section>
    
    <section class="chats-box">
        <ul>
            <?php if( $customers !== [] ) : ?>
                <table>
                    <thead>
                        <tr>
                            <th>Nom</th>
                            <th>Pénom</th>
                            <th>Email</th>
                            <th>Date de naissance</th>
                            <th>Adresse</th>
                            <th>Statut</th>
                            <th colspan=2>Fonctions</th>
                        </tr>
                    </thead>
                    <tbody>
                    <?php foreach( $customers as $customer) : ?>
                        <tr>
                            <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>><?= htmlspecialchars($customer['lastname']) ?></td>
                            <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>><?= htmlspecialchars($customer['firstname']) ?></td>
                            <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>><?= htmlspecialchars($customer['email']) ?></td>
                            <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>><?= htmlspecialchars(date_format(date_create($customer['birthday']), 'd-m-Y')) ?></td>
                            <?php if( $customer['adress'] !== null ) : ?>
                                <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>><?= htmlspecialchars($customer['adress'] . ' '. $customer['zipcode'] . ' ' . $customer['city'] . ' [' . $customer['pays'] . ']') ?></td>
                            <?php else : ?>
                                <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>>Pas d'adresse.</td>
                            <?php endif; ?>
                            <td <?php if($customer['statut']=='COMPTE SUSPENDU'){ echo'class="inactif"'; }?>><?= htmlspecialchars($customer['statut']) ?></td>
                            <td><a href="index.php?route=updateCustomer&id=<?= $customer['id'] ?>" class="btn colorYellow">Modifier</a></td>
                            <td><a href="index.php?route=displayOneCustomer&id=<?= $customer['id'] ?>" class="btn colorGreen">Voir</a></td>
    
                        </tr>
                    <?php endforeach; ?>
                    </tbody>
                </table>
            <?php else : ?>
                <li>Cette section est vide.</li>
            <?php endif; ?>
        </ul>
    </section>    
</main>
